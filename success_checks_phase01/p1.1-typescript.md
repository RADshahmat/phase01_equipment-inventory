export interface Device {
    host: string;
}

export interface LogEntry {
    host: string;
    statusCode: number | "DOWN" | "INVALID_URL";
    ms: number;
    downStreak?: number;
    alert?: boolean;
}

github repo : [Repository](https://github.com/RADshahmat/ping-logger-cli-phase0)